 string reverseWord(string str)
    {
       int n = str.size();
       
       int s = 0, e = n-1;
       while(s < e)
       {
           swap(str[s++], str[e--]);
       }
       
       return str;
    }
