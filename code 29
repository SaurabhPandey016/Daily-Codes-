#include <bits/stdc++.h>

using namespace std;

int main()
{
	int T;
	cin >> T;
	while (T--)
	{
		int n;
		cin>>n;
		int arr[n];
		int mini=INT32_MAX;
		for (int i = 0; i < n; i++)
		{
			cin>>arr[i];
			if(mini>arr[i]) mini=arr[i];
		}		
		cout<<mini<<endl;
	}
	return 0;
}
