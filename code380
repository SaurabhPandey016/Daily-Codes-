int mod=1000000007;
    int firstElement(int n) {
        int first=1,sec=1,sum=1;
        for(int i=0;i<n-2;i++)
        {
            sum=(first%mod)+(sec%mod);
            first=sec%mod;
            sec=sum%mod;
        }
        return s;
    }
